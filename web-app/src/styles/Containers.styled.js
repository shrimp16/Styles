import styled from 'styled-components';

export const ButtonsContainer = styled.div`
    display: flex;
    flex-direction: row;
    width: fit-content;
    margin: auto;

    * {
        margin: 5px;
    }
`